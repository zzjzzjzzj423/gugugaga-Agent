1. 更改原有coding agent的提示词
2. 带证据门控的agent loop
搜索候选网页
    ↓
Fetch 并注册真实来源
    ↓
模型准备提交最终答案
    ↓
Evidence Finalization Gate
    ├─ 通过 → 返回报告
    └─ 不通过
         ├─ 第一次 → 告诉模型缺什么，允许修复一次
         └─ 第二次 → INSUFFICIENT_EVIDENCE

什么是Evidence Finalization Gate
主要有两个函数构成
a. 证据校验函数(validate_research_final())
    a.1 成功 Fetch 的来源一共来自多少个不同网站域名。
    就是把所有成功 Fetch URL 中的网站域名提取出来、自动去重，用于检查 Agent 是否真正进行了跨网站交叉验证。
    a.2 最终答案引用核验, 输入两个参数final_text, registered_sources。
    a.3 加入到agentloop中

最多可以修复一次